# Core API framework
fastapi==0.115.5
uvicorn[standard]==0.30.6

# Data validation
pydantic>=2.7,<3

# HTTP client for health checks
httpx>=0.27.0

# Optional: roslibpy if backend needs ROS access via rosbridge
# roslibpy>=0.7.0

# NOTE: Do NOT install rclpy - ROS2 runs in Docker container.
# Frontend connects to ROS via rosbridge_websocket.
# If backend needs ROS access, use roslibpy (not rclpy).




