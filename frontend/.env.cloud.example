# Environment Variables for Cloud Deployment

# === Cloud Mode (Production) ===
# Use cloud backend WebSocket instead of direct rosbridge
VITE_USE_CLOUD_MODE=true
VITE_CLOUD_BACKEND_URL=wss://your-app.render.com/ui
VITE_ROBOT_ID=mentorpi

# === Direct Mode (Local Development) ===
# Connect directly to robot's rosbridge (legacy mode)
# VITE_USE_CLOUD_MODE=false
# VITE_ROSBRIDGE_URL=ws://fordward.local:9090
# VITE_VIDEO_BASE=http://fordward.local:8080
# VITE_ROSBRIDGE_FALLBACK_URL=ws://192.168.1.X:9090
# VITE_VIDEO_FALLBACK_BASE=http://192.168.1.X:8080
